<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
    <!--
    <sql-query name="findTeamByName">
        <return-scalar column="name" type="java.lang.String"></return-scalar>
        <return-scalar column="isStrong" type="java.lang.Boolean"></return-scalar>
        <return-scalar column="name" type="java.math.BigDecimal"></return-scalar>
        <return-scalar column="name" type="java.math.BigDecimal"></return-scalar>
        <return-scalar column="name" type="java.math.BigDecimal"></return-scalar>

        select t.name      as name,
        t.is_strong        as isStrong,
        CAST(SUM(tc.technical)  / COUNT(*) AS decimal)  AS avgTechnical,
        CAST(SUM(tc.reputation) / COUNT(*) AS decimal)  AS avgReputation,
        CAST(SUM(tc.attitude)   / COUNT(*) AS decimal)  AS avgAttitude
        from team t
        inner join team_comments tc on t.id = tc.team_id
        where t.name like :name
        and t.is_recruit = 1
    </sql-query>
    -->
</hibernate-mapping>